<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.1.xsd">

    <!-- 开启自动扫包
    <context:component-scan base-package="com.cubeGo.*">
        制定扫包规则，不扫描@Controller注解的JAVA类，其他的还是要扫描

    </context:component-scan>-->
    <!-- 加载数据库连接配置 -->
    <bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:datasource.properties</value>
            </list>
        </property>
    </bean>
    <!-- 配置数据源 -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driver}"></property>
        <property name="url" value="${jdbc.url}"></property>
        <property name="username" value="${jdbc.username}"></property>
        <property name="password" value="${jdbc.password}"></property>
    </bean>

    <!-- 导入mybatis配置 -->
    <import resource="classpath:springContext-mybatis.xml"/>
    <!-- 导入jpa配置-->
    <import resource="classpath:springContext-JPA.xml"/>

    <!-- 启动AOP支持 -->
    <aop:aspectj-autoproxy/>
    <aop:config>
        <!-- 配置一个切入点
        <aop:pointcut id="serviceMethods" expression="execution(* com.test.ssm.service.impl.*ServiceImpl.*(..))" />
        <aop:advisor advice-ref="txAdvice" pointcut-ref="serviceMethods" />-->
    </aop:config>

</beans>